export interface Assistant {
  id: string;
  name: string;
  // Add other properties as needed
}

export interface Chat {
  id: string;
  name: string;
}
export interface Model {
  id: string;
  name: string;
}